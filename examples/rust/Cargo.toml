[package]
name = "identifold-example-rust"
version = "1.0.0"
edition = "2024"
publish = false

[dependencies]
identifold = { path = "../../packages/rust", features = ["postgres"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tokio-postgres = "0.7"
